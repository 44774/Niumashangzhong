<view class="empty">
  <view class="empty-icon"></view>
  <text class="empty-title">{{title}}</text>
  <text class="empty-desc" wx:if="{{desc}}">{{desc}}</text>
  <button wx:if="{{actionText}}" class="btn-secondary empty-action" bindtap="onAction">
    {{actionText}}
  </button>
</view>
