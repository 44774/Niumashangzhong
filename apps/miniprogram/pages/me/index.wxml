<view class="page">
  <view class="card profile">
    <image wx:if="{{user.avatarUrl}}" class="avatar-img" src="{{user.avatarUrl}}" mode="aspectFill" />
    <view wx:else class="avatar">{{avatarText}}</view>
    <view class="profile-info">
      <text class="profile-name">{{user.displayName || '未登录'}}</text>
      <text class="profile-sub">{{workspace.name}}</text>
    </view>
  </view>

  <view class="card menu">
    <view class="menu-item" bindtap="goShiftManage">
      <text>班次管理</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="goChangeRecords">
      <text>改班记录</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="goShareSettings">
      <text>分享设置</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="goNotify">
      <text>提醒设置</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="goLocation">
      <text>位置设置</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="goSchedules">
      <text>排班表管理</text>
      <text class="menu-arrow">›</text>
    </view>
  </view>

  <view class="card">
    <view class="menu-item" bindtap="showAbout">
      <text>关于与隐私</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="goPrivacyAgreement">
      <text>用户隐私协议</text>
      <text class="menu-arrow">›</text>
    </view>
  </view>

  <button class="btn-secondary btn-block logout-btn" bindtap="logout">退出登录</button>
  <text class="text-tertiary version">{{appName}} {{version}} · 个人模式</text>
</view>
